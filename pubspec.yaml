name: returning_home
description: It's an app to use when you get home.
version: 1.0.0+1

environment:
  sdk: ">=2.9.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core:
  firebase_analytics:
  firebase_auth:
  firebase_messaging:
  cloud_firestore:
  geoflutterfire:
  google_maps_flutter:
  geolocator:
  rxdart:
  provider:
  cupertino_icons:
  flutter_sfsymbols:
  flutter_hooks:
  hooks_riverpod:
  get_it:
  dio:
  dio_http_cache:
  enum_to_string:

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic_mono:
flutter:
  uses-material-design: true
