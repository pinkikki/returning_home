name: returning_home
description: It's an app to use when you get home.
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core:
  firebase_analytics:
  firebase_auth:
  cloud_firestore:
  geoflutterfire:
  google_maps_flutter:
  geolocator:
  rxdart:
  provider:
  cupertino_icons:

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
